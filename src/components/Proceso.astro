<section id="proceso" class="py-20 bg-bg-main">
  <div class="max-w-6xl mx-auto px-6 text-center space-y-16">
    <!-- Header -->
    <div class="space-y-4 max-w-2xl mx-auto">
      <h2 class="text-3xl md:text-4xl font-bold">Nuestro proceso de trabajo</h2>
      <p class="text-text-secondary">
        Implementamos soluciones tecnológicas de forma clara, eficiente y sin
        complicaciones.
      </p>
    </div>

    <!-- Steps -->
    <div class="grid md:grid-cols-4 gap-8">
      <!-- Step -->
      <div
        class="relative card text-left p-6 group hover:border-primary reveal reveal-show delay-100 hover:scale-105 transition"
      >
        <span class="text-4xl font-bold text-primary/20 absolute top-4 right-4">
          01
        </span>

        <h3 class="text-primary font-semibold mb-2">Evaluación</h3>

        <p class="text-text-secondary text-sm">
          Analizamos tu negocio, necesidades y posibles riesgos.
        </p>
      </div>

      <!-- Step -->
      <div
        class="relative card text-left p-6 group hover:border-primary reveal reveal-show delay-200 hover:scale-105 transition"
      >
        <span class="text-4xl font-bold text-primary/20 absolute top-4 right-4">
          02
        </span>

        <h3 class="text-primary font-semibold mb-2">Diseño de solución</h3>

        <p class="text-text-secondary text-sm">
          Proponemos una solución personalizada y eficiente.
        </p>
      </div>

      <!-- Step -->
      <div
        class="relative card text-left p-6 group hover:border-primary reveal reveal-show delay-300 hover:scale-105 transition"
      >
        <span class="text-4xl font-bold text-primary/20 absolute top-4 right-4">
          03
        </span>

        <h3 class="text-primary font-semibold mb-2">Instalación</h3>

        <p class="text-text-secondary text-sm">
          Implementamos todo de forma profesional y sin interrupciones.
        </p>
      </div>

      <!-- Step -->
      <div
        class="relative card text-left p-6 group hover:border-primary reveal reveal-show delay-400 hover:scale-105 transition"
      >
        <span class="text-4xl font-bold text-primary/20 absolute top-4 right-4">
          04
        </span>

        <h3 class="text-primary font-semibold mb-2">Soporte continuo</h3>

        <p class="text-text-secondary text-sm">
          Te acompañamos para mantener todo funcionando correctamente.
        </p>
      </div>
    </div>
  </div>
</section>
